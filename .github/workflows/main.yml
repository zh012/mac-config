name: OTA Tours

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  cron:
    runs-on: ubuntu-latest

    steps:
      - name: Run a one-line script
        run: curl -L -c deta-cookies https://google.com
